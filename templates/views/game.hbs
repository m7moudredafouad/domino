{{>header}}
    <input type="text" name="room" id="room" value="{{room}}" hidden>
    <input type="text" name="name" id="name" value="{{name}}" hidden>

    <div class="nav">
        <button class="btn" id="start">New Game</button>
        <div class="score">
            {{!-- <p id="playerScore">You: 0</p> --}}
            {{!-- <p id="AIScore">Computer: 0</p> --}}
        </div>
            <button class="btn" id="getCard">Get Card</button>
    </div>

    <div class="container">
        <div class="AICards"></div>
        <div class="playGround"></div>
        <div class="cont">
        </div>
        <div class="remain"></div>
    </div>
    

    <script src="/socket.io/socket.io.js"></script>
    <script src="/multi.js"></script>

{{>footer}}